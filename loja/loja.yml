spring:
  application:
    name: 'fornecedor'
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/loja?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
